/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.dashboard.hauscenter.com.bo']
    },
    assetPrefix: 'https://www.testing.hauscenter.com.bo',
}

module.exports = nextConfig
